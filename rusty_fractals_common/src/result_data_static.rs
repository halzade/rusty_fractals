use crate::area::Area;

pub struct ResultData {
    pub pixels: [f64; 2],
    pub area_result: Area,
}

impl ResultData {
    pub fn remove_elements_outside(&mut self) {

    }

    pub fn add_calculation_path_now(&mut self, path: [f64; 2]) {

    }
}